# Server Configuration
PORT=8000
PYTHONUNBUFFERED=1

# Model Configuration
MODEL_PATH=checkpoints/best_model.pt
CLASS_MAP_PATH=processed_data/class_map.json

# Environment (development or production)
ENVIRONMENT=production

# CORS Settings (comma-separated origins)
CORS_ORIGINS=*
