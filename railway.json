{
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "/bin/bash -c 'uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 4'",
    "restartPolicyMaxRetries": 5
  },
  "environment": {
    "PORT": "8000"
  }
}
